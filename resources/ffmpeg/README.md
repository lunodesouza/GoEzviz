# Bundled FFmpeg (not committed)

Download the Windows essentials build into this folder:

```powershell
./scripts/fetch-ffmpeg-windows.ps1
```

That places `ffmpeg.exe`, `ffplay.exe`, and `ffprobe.exe` here. The Windows zip next to `GoEzviz.exe` uses the same files so users do not need to install FFmpeg.
